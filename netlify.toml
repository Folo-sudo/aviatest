# Netlify (historique / secours pendant la migration vers Vercel)
# Ne pas supprimer tant que le site Netlify n'est pas decommissionne.
# Workflow actuel : docs/vercel-workflow.md

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
